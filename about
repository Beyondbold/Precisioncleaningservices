<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>About | Precision Cleaning Services</title>
<meta name="description" content="Reliable, detail-oriented, professional cleaning and restoration — what Precision Cleaning Services stands for and how a job gets done.">
<style>
/* =========================================================
   PRECISION CLEANING SERVICES — design tokens
   Direction: inspection / spec-sheet precision.
   Blueprint-grid, corner-bracket framing, mono data labels,
   and a literal "wipe clean" interactive reveal as the
   signature moment.
   ========================================================= */

@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&family=IBM+Plex+Mono:wght@500;600&display=swap');

:root{
  --ink: #0F2438;
  --ink-70: rgba(15,36,56,.72);
  --ink-45: rgba(15,36,56,.45);
  --paper: #F6F8F7;
  --paper-dim: #EBEFEE;
  --panel: #FFFFFF;
  --steel: #4A5D6B;
  --precision: #1C6E63;
  --precision-dark: #124F47;
  --precision-tint: #E4EFEC;
  --clean: #52A9A0;
  --alert: #C6541D;
  --alert-dark: #9C3F14;
  --alert-tint: #F6E7DE;
  --line: rgba(15,36,56,.14);
  --line-strong: rgba(15,36,56,.28);

  --display: 'Space Grotesk', 'Arial Narrow', sans-serif;
  --body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --mono: 'IBM Plex Mono', 'Courier New', monospace;

  --maxw: 1180px;
  --edge: clamp(20px, 5vw, 64px);
}

*,*::before,*::after{ box-sizing:border-box; }
html{ scroll-behavior:smooth; }
body{
  margin:0;
  background:var(--paper);
  color:var(--ink);
  font-family:var(--body);
  font-size:16px;
  line-height:1.55;
  -webkit-font-smoothing:antialiased;
}
img{ max-width:100%; display:block; }
a{ color:inherit; text-decoration:none; }
h1,h2,h3,h4{ font-family:var(--display); margin:0; letter-spacing:-.01em; }
p{ margin:0; }
ul{ margin:0; padding:0; list-style:none; }
button{ font-family:inherit; cursor:pointer; }

@media (prefers-reduced-motion: reduce){
  *{ animation-duration:.001ms !important; animation-iteration-count:1 !important; transition-duration:.001ms !important; scroll-behavior:auto !important; }
}

:focus-visible{ outline:2px solid var(--precision); outline-offset:3px; }

.wrap{ max-width:var(--maxw); margin:0 auto; padding:0 var(--edge); }

/* ---------- eyebrow / mono data labels ---------- */
.eyebrow{
  font-family:var(--mono);
  font-size:12px;
  font-weight:600;
  letter-spacing:.14em;
  text-transform:uppercase;
  color:var(--precision);
  display:flex;
  align-items:center;
  gap:10px;
}
.eyebrow::before{
  content:"";
  width:18px; height:1px;
  background:var(--precision);
}

/* ---------- blueprint grid backdrop ---------- */
.grid-bg{
  position:relative;
  background-image:
    linear-gradient(var(--line) 1px, transparent 1px),
    linear-gradient(90deg, var(--line) 1px, transparent 1px);
  background-size:40px 40px;
  background-position:center top;
}

/* ---------- corner bracket frame (signature framing device) ---------- */
.bracket{ position:relative; }
.bracket::before,.bracket::after,
.bracket .bk-tr,.bracket .bk-bl{
  content:""; position:absolute; width:22px; height:22px;
  border:2px solid var(--precision);
  opacity:.9;
}
.bracket::before{ top:-1px; left:-1px; border-right:none; border-bottom:none; }
.bracket::after{ bottom:-1px; right:-1px; border-left:none; border-top:none; }
.bracket .bk-tr{ top:-1px; right:-1px; border-left:none; border-bottom:none; }
.bracket .bk-bl{ bottom:-1px; left:-1px; border-right:none; border-top:none; }

/* =========================== HEADER =========================== */
.site-header{
  position:sticky; top:0; z-index:50;
  background:rgba(246,248,247,.92);
  backdrop-filter:blur(10px);
  border-bottom:1px solid var(--line);
}
.header-bar{
  display:flex; align-items:center; justify-content:space-between;
  padding:16px var(--edge);
  max-width:var(--maxw); margin:0 auto;
  gap:24px;
}
.logo{ display:flex; align-items:center; gap:12px; }
.logo-mark{
  width:38px; height:38px; flex:none;
  border:2px solid var(--ink);
  position:relative;
  display:grid; place-items:center;
}
.logo-mark span{
  width:14px; height:14px; background:var(--precision);
}
.logo-text{ display:flex; flex-direction:column; line-height:1.1; }
.logo-text b{ font-family:var(--display); font-weight:700; font-size:16.5px; letter-spacing:.01em; }
.logo-text small{ font-family:var(--mono); font-size:10px; letter-spacing:.12em; color:var(--steel); text-transform:uppercase; }

.main-nav{ display:flex; align-items:center; gap:30px; }
.main-nav a{
  font-size:14.5px; font-weight:500; color:var(--ink-70);
  padding:6px 2px; border-bottom:2px solid transparent;
}
.main-nav a:hover, .main-nav a[aria-current="page"]{ color:var(--ink); border-color:var(--precision); }

.header-actions{ display:flex; align-items:center; gap:12px; }
.header-phone{
  font-family:var(--mono); font-size:13.5px; font-weight:600;
  display:flex; align-items:center; gap:8px; color:var(--ink);
}
.header-phone svg{ width:15px; height:15px; stroke:var(--alert); }

.nav-toggle{
  display:none; background:none; border:1px solid var(--line-strong);
  width:40px; height:40px; align-items:center; justify-content:center;
}
.nav-toggle span, .nav-toggle span::before, .nav-toggle span::after{
  content:""; display:block; width:18px; height:2px; background:var(--ink); position:relative;
}
.nav-toggle span::before{ position:absolute; top:-6px; }
.nav-toggle span::after{ position:absolute; top:6px; }

/* ---------- buttons ---------- */
.btn{
  display:inline-flex; align-items:center; justify-content:center; gap:8px;
  font-family:var(--body); font-weight:600; font-size:14.5px;
  padding:12px 22px; border:1.5px solid transparent;
  white-space:nowrap;
}
.btn-primary{ background:var(--alert); color:#fff; }
.btn-primary:hover{ background:var(--alert-dark); }
.btn-outline{ background:transparent; border-color:var(--ink); color:var(--ink); }
.btn-outline:hover{ background:var(--ink); color:var(--paper); }
.btn-ghost-light{ background:transparent; border-color:rgba(255,255,255,.5); color:#fff; }
.btn-ghost-light:hover{ background:rgba(255,255,255,.12); }
.btn-block{ width:100%; }
.btn-lg{ padding:15px 28px; font-size:15.5px; }

/* =========================== HERO ============================ */
.hero{
  padding:64px var(--edge) 0;
  max-width:var(--maxw); margin:0 auto;
  display:grid; grid-template-columns:1.05fr .95fr; gap:56px; align-items:center;
}
.hero-kicker{ margin-bottom:22px; }
.hero h1{
  font-size:clamp(34px,4.4vw,56px);
  font-weight:700; line-height:1.04; color:var(--ink);
}
.hero h1 em{ font-style:normal; color:var(--precision); }
.hero-sub{
  margin-top:20px; font-size:17px; color:var(--steel); max-width:46ch; line-height:1.6;
}
.hero-actions{ display:flex; gap:14px; margin-top:32px; flex-wrap:wrap; }
.hero-trust{
  margin-top:38px; display:flex; gap:28px; flex-wrap:wrap;
  padding-top:22px; border-top:1px solid var(--line);
}
.trust-stat b{ display:block; font-family:var(--display); font-size:22px; color:var(--ink); }
.trust-stat span{ font-family:var(--mono); font-size:11px; letter-spacing:.08em; text-transform:uppercase; color:var(--steel); }

/* ---------- signature: wipe-reveal panel ---------- */
.reveal{
  position:relative; aspect-ratio:4/5; overflow:hidden;
  border:1px solid var(--line-strong);
}
.reveal-layer{ position:absolute; inset:0; }
.reveal-after{
  background:
    radial-gradient(120% 90% at 20% 0%, #1E7A6D 0%, var(--precision-dark) 62%, #0D3630 100%);
}
.reveal-after::after{
  content:""; position:absolute; inset:0; opacity:.5;
  background:
    linear-gradient(var(--line) 1px, transparent 1px),
    linear-gradient(90deg, var(--line) 1px, transparent 1px);
  background-size:26px 26px; mix-blend-mode:overlay;
}
.reveal-before{
  background:
    radial-gradient(120% 90% at 20% 0%, #6B5A46 0%, #3E3428 62%, #241E17 100%);
  clip-path:inset(0 45% 0 0);
}
.reveal-before::after{
  content:""; position:absolute; inset:0; opacity:.55;
  background-image:
    repeating-linear-gradient(115deg, rgba(0,0,0,.18) 0 2px, transparent 2px 7px),
    repeating-linear-gradient(25deg, rgba(0,0,0,.12) 0 2px, transparent 2px 9px);
}
.reveal-tag{
  position:absolute; top:16px; font-family:var(--mono); font-size:10.5px;
  letter-spacing:.1em; text-transform:uppercase; color:#fff; padding:5px 9px;
  background:rgba(15,36,56,.55); backdrop-filter:blur(3px);
}
.reveal-tag.before{ left:16px; }
.reveal-tag.after{ right:16px; }
.reveal-readout{
  position:absolute; left:16px; bottom:16px; z-index:3;
  font-family:var(--mono); font-size:11px; color:#fff;
  background:rgba(15,36,56,.55); backdrop-filter:blur(3px);
  padding:8px 10px; line-height:1.6; letter-spacing:.02em;
}
.reveal-slider-track{
  position:absolute; inset:0; z-index:4;
  display:flex; align-items:center;
}
.reveal-slider{
  -webkit-appearance:none; appearance:none;
  width:100%; height:100%; margin:0; background:transparent; position:relative;
}
.reveal-slider::-webkit-slider-runnable-track{ background:transparent; height:100%; }
.reveal-slider::-moz-range-track{ background:transparent; height:100%; }
.reveal-slider::-webkit-slider-thumb{
  -webkit-appearance:none; appearance:none;
  width:38px; height:38px; margin-top:0;
  background:#fff; border:2px solid var(--precision);
  cursor:ew-resize;
  box-shadow:0 2px 10px rgba(15,36,56,.35);
}
.reveal-slider::-moz-range-thumb{
  width:36px; height:36px; border-radius:0;
  background:#fff; border:2px solid var(--precision); cursor:ew-resize;
}
.reveal-line{
  position:absolute; top:0; bottom:0; width:2px; background:#fff;
  box-shadow:0 0 0 4px rgba(255,255,255,.18); z-index:2; pointer-events:none;
}

/* =========================== SECTIONS ============================ */
section{ padding:88px var(--edge); }
.section-in{ max-width:var(--maxw); margin:0 auto; }
.section-head{ display:flex; justify-content:space-between; align-items:flex-end; gap:32px; margin-bottom:48px; flex-wrap:wrap; }
.section-head h2{ font-size:clamp(26px,3vw,36px); margin-top:14px; max-width:22ch; }
.section-head p{ max-width:42ch; color:var(--steel); font-size:15.5px; padding-bottom:4px; }

.band-dark{ background:var(--ink); color:var(--paper); }
.band-dark .eyebrow{ color:var(--clean); }
.band-dark .eyebrow::before{ background:var(--clean); }
.band-tint{ background:var(--precision-tint); }

/* ---------- service cards ---------- */
.svc-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:1px; background:var(--line-strong); border:1px solid var(--line-strong); }
.svc-card{ background:var(--panel); padding:32px 28px; display:flex; flex-direction:column; gap:14px; min-height:220px; }
.svc-num{ font-family:var(--mono); font-size:11px; color:var(--precision); letter-spacing:.08em; }
.svc-card h3{ font-size:19px; }
.svc-card p{ color:var(--steel); font-size:14.5px; line-height:1.6; }
.svc-tag{ margin-top:auto; font-family:var(--mono); font-size:10.5px; letter-spacing:.06em; text-transform:uppercase; color:var(--ink-45); padding-top:10px; border-top:1px solid var(--line); }
.svc-tag.urgent{ color:var(--alert); }

/* ---------- process / steps ---------- */
.steps{ display:grid; grid-template-columns:repeat(4,1fr); gap:28px; }
.step{ border-top:2px solid var(--ink); padding-top:16px; }
.step .eyebrow{ margin-bottom:10px; }
.step h4{ font-size:17px; margin-bottom:8px; }
.step p{ color:var(--steel); font-size:14px; }

/* ---------- why / values ---------- */
.value-grid{ display:grid; grid-template-columns:repeat(4,1fr); gap:24px; }
.value{ padding:24px 0; border-top:1px solid var(--line); }
.value h4{ font-size:16px; margin:10px 0 8px; }
.value p{ font-size:14px; color:var(--steel); }

/* ---------- testimonials ---------- */
.quote-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:24px; }
.quote-card{ background:var(--panel); border:1px solid var(--line); padding:26px; display:flex; flex-direction:column; gap:16px; }
.quote-card p{ font-size:15px; line-height:1.6; }
.quote-stars{ font-family:var(--mono); color:var(--alert); font-size:13px; letter-spacing:.1em; }
.quote-who{ font-family:var(--mono); font-size:11.5px; color:var(--steel); text-transform:uppercase; letter-spacing:.06em; }

/* ---------- CTA band ---------- */
.cta-band{
  background:var(--ink); color:var(--paper); padding:72px var(--edge);
}
.cta-in{ max-width:var(--maxw); margin:0 auto; display:flex; justify-content:space-between; align-items:center; gap:32px; flex-wrap:wrap; }
.cta-in h2{ font-size:clamp(24px,3vw,34px); max-width:20ch; }
.cta-in p{ color:rgba(246,248,247,.7); margin-top:10px; max-width:44ch; }
.cta-actions{ display:flex; gap:14px; flex-wrap:wrap; }

/* =========================== FORM =========================== */
.form-grid{ display:grid; grid-template-columns:1.1fr .9fr; gap:56px; align-items:flex-start; }
.field{ margin-bottom:20px; }
.field label{ display:block; font-family:var(--mono); font-size:11.5px; letter-spacing:.06em; text-transform:uppercase; color:var(--steel); margin-bottom:8px; }
.field input, .field select, .field textarea{
  width:100%; border:1.5px solid var(--line-strong); background:var(--panel);
  padding:12px 14px; font-family:var(--body); font-size:15px; color:var(--ink);
}
.field input:focus, .field select:focus, .field textarea:focus{ border-color:var(--precision); }
.field textarea{ resize:vertical; min-height:110px; }
.field-row{ display:grid; grid-template-columns:1fr 1fr; gap:16px; }
.field-file{ border:1.5px dashed var(--line-strong); padding:20px; text-align:center; font-size:13.5px; color:var(--steel); }
.form-note{ font-size:12.5px; color:var(--ink-45); margin-top:6px; }
.form-status{ display:none; margin-top:16px; padding:12px 14px; font-size:14px; border:1px solid var(--precision); background:var(--precision-tint); }
.form-status.show{ display:block; }

.info-card{ background:var(--panel); border:1px solid var(--line); padding:28px; }
.info-row{ display:flex; gap:14px; padding:16px 0; border-top:1px solid var(--line); }
.info-row:first-child{ border-top:none; padding-top:0; }
.info-row svg{ width:18px; height:18px; stroke:var(--precision); flex:none; margin-top:2px; }
.info-row b{ display:block; font-size:14.5px; margin-bottom:2px; }
.info-row span{ font-size:13.5px; color:var(--steel); }
.emergency-note{ margin-top:20px; padding:16px; background:var(--alert-tint); border-left:3px solid var(--alert); font-size:13.5px; }
.emergency-note b{ display:block; color:var(--alert-dark); font-family:var(--mono); font-size:11.5px; letter-spacing:.06em; text-transform:uppercase; margin-bottom:4px; }

/* =========================== FOOTER =========================== */
.site-footer{ background:var(--ink); color:var(--paper); padding:64px var(--edge) 28px; }
.footer-grid{ max-width:var(--maxw); margin:0 auto; display:grid; grid-template-columns:1.4fr 1fr 1fr 1fr; gap:40px; }
.footer-grid .logo-text b{ color:#fff; }
.footer-grid .logo-mark{ border-color:#fff; }
.footer-desc{ color:rgba(246,248,247,.6); font-size:14px; margin-top:16px; max-width:32ch; line-height:1.6; }
.footer-social{ display:flex; gap:10px; margin-top:20px; }
.footer-social a{ width:36px; height:36px; border:1px solid rgba(246,248,247,.3); display:grid; place-items:center; }
.footer-social a:hover{ border-color:var(--clean); }
.footer-social svg{ width:16px; height:16px; stroke:#fff; }
.footer-col h5{ font-family:var(--mono); font-size:11px; letter-spacing:.1em; text-transform:uppercase; color:var(--clean); margin-bottom:16px; }
.footer-col li{ margin-bottom:10px; }
.footer-col a{ font-size:14px; color:rgba(246,248,247,.75); }
.footer-col a:hover{ color:#fff; }
.footer-bottom{
  max-width:var(--maxw); margin:48px auto 0; padding-top:22px; border-top:1px solid rgba(246,248,247,.15);
  display:flex; justify-content:space-between; font-size:12.5px; color:rgba(246,248,247,.45); flex-wrap:wrap; gap:10px;
  font-family:var(--mono);
}

/* =========================== MESSENGER FAB =========================== */
.fb-fab{
  position:fixed; right:22px; bottom:22px; z-index:60;
  display:flex; align-items:center; gap:10px;
  background:var(--ink); color:#fff; padding:13px 18px;
  border:1px solid rgba(255,255,255,.15);
  box-shadow:0 8px 24px rgba(15,36,56,.28);
}
.fb-fab svg{ width:19px; height:19px; }
.fb-fab span{ font-size:13.5px; font-weight:600; }
.fb-fab:hover{ background:var(--precision-dark); }

/* =========================== PAGE HEADER (inner pages) === */
.page-head{ padding:56px var(--edge) 40px; max-width:var(--maxw); margin:0 auto; }
.page-head h1{ font-size:clamp(30px,4vw,46px); margin-top:16px; max-width:20ch; }
.page-head p{ margin-top:16px; color:var(--steel); font-size:16px; max-width:56ch; }

.split{ display:grid; grid-template-columns:1fr 1fr; gap:56px; align-items:center; }
.split img, .split .ph{ width:100%; }
.ph{ aspect-ratio:4/3; background:var(--paper-dim); border:1px solid var(--line); display:grid; place-items:center; }
.ph span{ font-family:var(--mono); font-size:11px; color:var(--ink-45); text-transform:uppercase; letter-spacing:.08em; }

.timeline{ border-left:2px solid var(--line-strong); padding-left:28px; display:flex; flex-direction:column; gap:36px; }
.tl-item{ position:relative; }
.tl-item::before{ content:""; position:absolute; left:-33px; top:2px; width:8px; height:8px; background:var(--precision); }
.tl-item .eyebrow{ margin-bottom:8px; }
.tl-item h4{ font-size:17px; margin-bottom:6px; }
.tl-item p{ color:var(--steel); font-size:14.5px; }

.badge-row{ display:flex; gap:16px; flex-wrap:wrap; }
.badge{ border:1px solid var(--line-strong); padding:12px 16px; font-family:var(--mono); font-size:11.5px; letter-spacing:.05em; text-transform:uppercase; color:var(--steel); }

/* =========================== RESPONSIVE =========================== */
@media (max-width: 980px){
  .main-nav{ display:none; }
  .nav-toggle{ display:flex; }
  .hero{ grid-template-columns:1fr; padding-top:40px; }
  .form-grid{ grid-template-columns:1fr; }
  .svc-grid{ grid-template-columns:repeat(2,1fr); }
  .steps{ grid-template-columns:repeat(2,1fr); }
  .value-grid{ grid-template-columns:repeat(2,1fr); }
  .quote-grid{ grid-template-columns:1fr; }
  .footer-grid{ grid-template-columns:1fr 1fr; }
  .split{ grid-template-columns:1fr; }
  .header-phone span{ display:none; }
}
@media (max-width: 620px){
  .svc-grid{ grid-template-columns:1fr; }
  .steps{ grid-template-columns:1fr; }
  .value-grid{ grid-template-columns:1fr; }
  .field-row{ grid-template-columns:1fr; }
  .footer-grid{ grid-template-columns:1fr; }
  .cta-in{ flex-direction:column; align-items:flex-start; }
  section{ padding:64px var(--edge); }
}

</style>
</head>
<body>

<header class="site-header">
  <div class="header-bar">
    <a class="logo" href="index.html">
      <div class="logo-mark"><span></span></div>
      <div class="logo-text"><b>PRECISION</b><small>Cleaning Services</small></div>
    </a>
    <nav class="main-nav" id="main-nav">
      <a href="index.html">Home</a>
      <a href="services.html">Services</a>
      <a href="about.html" aria-current="page">About</a>
      <a href="contact.html">Contact</a>
    </nav>
    <div class="header-actions">
      <a class="header-phone" href="tel:+12072279133">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.362 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.338 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
        <span>(207) 227-9133</span>
      </a>
      <a class="btn btn-primary" href="contact.html">Get a Free Quote</a>
      <button class="nav-toggle" aria-label="Toggle menu" aria-expanded="false"><span></span></button>
    </div>
  </div>
</header>

<div class="page-head grid-bg">
  <div class="eyebrow">About Precision Cleaning Services</div>
  <h1>Built on showing up, and doing it right when we get there.</h1>
  <p>Precision Cleaning Services is owned and operated by Caleb Schwartz, serving Aroostook and Penobscot County. [Placeholder — replace with real background: how long you've operated, what led you to start the business, and what you want clients in the area to know.]</p>
</div>

<section>
  <div class="section-in split">
    <div class="ph bracket"><span class="bk-tr"></span><span class="bk-bl"></span><span>Photo: Caleb or crew on a job site</span></div>
    <div>
      <div class="eyebrow">Our Approach</div>
      <h2 style="font-size:clamp(24px,3vw,32px); margin-top:14px;">Every job runs on a checklist, not a guess.</h2>
      <p style="margin-top:16px; color:var(--steel); line-height:1.7;">Cleaning and restoration work is easy to do halfway. We don't. Every job — from a same-day water call to a scheduled office contract — follows a documented process, so the outcome doesn't depend on which day of the week it is.</p>
      <p style="margin-top:14px; color:var(--steel); line-height:1.7;">[Placeholder — add specifics on Caleb's background: years in business, certifications, equipment, or what sets the process apart.]</p>
    </div>
  </div>
</section>

<section class="band-tint">
  <div class="section-in">
    <div class="section-head">
      <div>
        <div class="eyebrow">What We Stand On</div>
        <h2>Four words we hold ourselves to.</h2>
      </div>
    </div>
    <div class="value-grid">
      <div class="value">
        <div class="eyebrow">Reliable</div>
        <h4>Confirmed appointments</h4>
        <p>You get a real time window and a crew that shows up in it — every time.</p>
      </div>
      <div class="value">
        <div class="eyebrow">Detail-Oriented</div>
        <h4>Nothing skipped</h4>
        <p>A documented scope for every job means the small stuff doesn't get missed.</p>
      </div>
      <div class="value">
        <div class="eyebrow">Professional</div>
        <h4>Licensed &amp; insured</h4>
        <p>Your home or business is protected and treated with respect on every visit.</p>
      </div>
      <div class="value">
        <div class="eyebrow">Consistent</div>
        <h4>Same result, every time</h4>
        <p>One-time job or standing contract — the standard doesn't change.</p>
      </div>
    </div>
  </div>
</section>

<section>
  <div class="section-in">
    <div class="section-head">
      <div>
        <div class="eyebrow">Credentials</div>
        <h2>[Add your real licenses &amp; certifications]</h2>
      </div>
      <p>Replace these placeholders with actual certifications — IICRC, state contractor license number, bonding/insurance carrier, etc.</p>
    </div>
    <div class="badge-row">
      <div class="badge">Licensed Contractor</div>
      <div class="badge">Fully Insured</div>
      <div class="badge">IICRC Certified [placeholder]</div>
      <div class="badge">Locally Owned &amp; Operated</div>
    </div>
  </div>
</section>

<section class="cta-band">
  <div class="cta-in">
    <div>
      <h2>Ready to get on the schedule?</h2>
      <p>Tell us about the job and we'll follow up with a clear quote and a real appointment time.</p>
    </div>
    <div class="cta-actions">
      <a class="btn btn-primary btn-lg" href="contact.html">Request a Free Quote</a>
    </div>
  </div>
</section>

<footer class="site-footer">
  <div class="footer-grid">
    <div>
      <a class="logo" href="index.html">
        <div class="logo-mark"><span></span></div>
        <div class="logo-text"><b>PRECISION</b><small>Cleaning Services</small></div>
      </a>
      <p class="footer-desc">Commercial and residential cleaning, mold remediation, and water damage remediation. Reliable, detail-oriented service you can schedule with confidence.</p>
      <div class="footer-social">
        <a href="https://facebook.com/PCScleaning1" target="_blank" rel="noopener" aria-label="Facebook">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg>
        </a>
      </div>
    </div>
    <div class="footer-col">
      <h5>Navigate</h5>
      <ul>
        <li><a href="index.html">Home</a></li>
        <li><a href="services.html">Services</a></li>
        <li><a href="about.html">About</a></li>
        <li><a href="contact.html">Get a Quote</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h5>Services</h5>
      <ul>
        <li><a href="services.html">Mold Remediation</a></li>
        <li><a href="services.html">Water Damage</a></li>
        <li><a href="services.html">Commercial Cleaning</a></li>
        <li><a href="services.html">Deep Cleaning</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h5>Contact</h5>
      <ul>
        <li><a href="tel:+12072279133">(207) 227-9133</a></li>
        <li><a href="mailto:info@precisioncleaningservices.co">info@precisioncleaningservices.co</a></li>
        <li><a href="https://m.me/PCScleaning1" target="_blank" rel="noopener">Message on Facebook</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span>© 2026 Precision Cleaning Services. All rights reserved.</span>
    <span>Serving Aroostook &amp; Penobscot County, Maine</span>
  </div>
</footer>

<a class="fb-fab" href="https://m.me/PCScleaning1" target="_blank" rel="noopener" aria-label="Message us on Facebook">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg>
  <span>Message Us</span>
</a>

<script>
// ===== Mobile nav toggle =====
document.addEventListener('DOMContentLoaded', function () {
  var toggle = document.querySelector('.nav-toggle');
  var nav = document.querySelector('.main-nav');
  if (toggle && nav) {
    toggle.addEventListener('click', function () {
      var isOpen = nav.classList.toggle('open');
      toggle.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
    });
  }

  // ===== Hero wipe-reveal slider (before/after) =====
  var slider = document.querySelector('.reveal-slider');
  var before = document.querySelector('.reveal-before');
  var line = document.querySelector('.reveal-line');
  if (slider && before) {
    var update = function () {
      var v = slider.value; // 0-100
      before.style.clipPath = 'inset(0 ' + (100 - v) + '% 0 0)';
      if (line) line.style.left = v + '%';
    };
    slider.addEventListener('input', update);
    update();
  }

  // ===== Quote form → Google Sheet via Apps Script Web App =====
  // The form posts into a hidden iframe (not a fetch call), so there is
  // no CORS issue talking to the Apps Script Web App from a different domain.
  var form = document.querySelector('.quote-form');
  if (form) {
    form.addEventListener('submit', function (e) {
      var status = form.querySelector('.form-status');
      var action = form.getAttribute('action') || '';

      // If the Apps Script URL hasn't been pasted in yet, don't let the
      // form fail silently — tell whoever's testing it what to do.
      if (action.indexOf('PASTE_YOUR_GOOGLE_SCRIPT_URL_HERE') !== -1) {
        e.preventDefault();
        if (status) {
          status.textContent = 'Form is not connected yet. Deploy the Google Apps Script (see apps-script.gs / setup steps) and paste the Web App URL into the form action in contact.html.';
          status.classList.add('show');
          status.style.borderColor = '#C6541D';
          status.style.background = '#F6E7DE';
        }
        return;
      }

      if (status) {
        status.textContent = 'Sending your request…';
        status.classList.add('show');
      }
      // Native submit proceeds into the hidden iframe — page never navigates away.
      // We can't read the cross-origin iframe's response, so we optimistically
      // confirm after a short delay and reset the form.
      setTimeout(function () {
        if (status) {
          status.textContent = 'Thanks — your request was sent. We\'ll be in touch shortly.';
          status.style.borderColor = '';
          status.style.background = '';
        }
        form.reset();
      }, 1200);
    });
  }
});

</script>
</body>
</html>
